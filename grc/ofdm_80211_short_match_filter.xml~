<?xml version="1.0"?>
<block>
  <name>short_match_filter</name>
  <key>ofdm_80211_short_match_filter</key>
  <category>ofdm_80211</category>
  <import>import ofdm_80211</import>
  <make>ofdm_80211.short_match_filter()</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>...</name>
    <key>...</key>
    <type>...</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->


	<sink>
		<name>in</name>
		<type>complex</type>
		<nports>1</nports>
	</sink>


	<source>
		<name>out</name>
		<type>complex</type>
		<nports>1</nports>
	</source>





</block>
